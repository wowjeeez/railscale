[package]
name = "conductor"
version = "0.1.0"
edition = "2024"

[dependencies]
train_track = {path = "../train_track"}
memchr = {workspace = true}
bytes = {workspace = true}
rayon = {workspace = true}